export const metadata = {
    title: "Favourite",
    description: "Favourite description"
}

export default function RootLayout ( {children} ) {
    return (
        <>
            {children}
        </>
    )
}